<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <record id="pos_config_view_form" model="ir.ui.view">
        <field name="name">pos.config.view.form.inherit.psae_pos_salesperson</field>
        <field name="model">pos.config</field>
        <field name="inherit_id" ref="point_of_sale.pos_config_view_form" />
        <field name="arch" type="xml">
            <xpath expr="//setting[@id='other_devices']" position="after">
                <setting>
                    <field name="is_salesperson_mandatory" />
                </setting>
                <setting>
                    <field name="selected_employee_ids" widget="many2many_tags" required="is_salesperson_mandatory"/>
                </setting>
            </xpath>
        </field>
    </record>
</odoo>
