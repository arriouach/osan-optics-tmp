<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="stock_location_inherit_allowed_users" model="ir.ui.view">
            <field name="name">stock.location.form.inherit.allowed.users</field>
            <field name="model">stock.location</field>
            <field name="inherit_id" ref="stock.view_location_form" />
            <field name="arch" type="xml">
                <group name="additional_info" position="inside">
                    <field name="user_ids" widget="many2many_tags" string="Allowed Users"
                        groups="base.group_system" readonly="usage == 'internal'" />
                </group>
            </field>
        </record>
    </data>
</odoo>
